// Vuetify's per-component source imports its own `.css` file directly (e.g.
// `vuetify/lib/components/VApp/VApp.css`). Jest cannot parse CSS as JS, so
// every such import is mapped to this empty module instead.
module.exports = {};
