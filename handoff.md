# Handoff: UI library comparison

**Updated:** 2026-09-04. Previously 2026-09-03.

## 1. What this is

The workspace behind the screen spec, held here as `spec/screen-spec.md`. Eight UI libraries build the same `/tickets` screen so they can be compared on ergonomics, bundle size, and accessibility defaults.

The spec was first written in the sibling folder `../demo-resume/` as `bakeoff-screen-spec.md` and copied here. As of 2026-09-04 that folder holds only `demo-resume.md`, `handoff.md` and `resume-skills-list.md`, and is not a git repository, so this repo's copy is the only one left. Nothing here should be treated as syncing from it.

Demo content. The domain, the data, and the numbers are fictional, matching `../demo-resume/demo-resume.md`. Git history starts at the phase one commit, which is the baseline every build diffs against.

Phase one is built and frozen. Phase two, the eight builds, is done: all eight of `builds/` are built and scored, and `results/` has all eight files. Every build passes all 18 criteria; `react-antd` passes all 18 but is over the 180 KB bundle budget. All eight builds are committed, one commit each, on top of a lockfile commit. The only uncommitted file as of 2026-09-04 is this one: the section 7 screen fixes, the dev server ports, the section 8 rename and the rescored `results/` all landed in `65756f2` and `751f0de`.

Verified on 2026-09-04, after the rename: `pnpm fixture:check` passes all ten assertions, `pnpm typecheck` passes, and `react-headless` passes 18 of 18 criteria in 18 seconds. The other seven suites were not re-run in that session; the rescore commit is the evidence for those.

On 2026-09-03 all eight dev servers were started at once and served their screens, verifying only that each app boots; nobody has clicked through the eight screens in a browser yet.

## 2. What exists

- `spec/screen-spec.md`: the spec. It was copied from the sibling folder's `bakeoff-screen-spec.md`, which no longer exists there, so this is now the only copy and the one the builds read.
- `packages/fixture`: 240 tickets from a seeded generator, committed as `tickets.json`. Three entry points: `@uilc/fixture` for `loadTickets` and types, `@uilc/fixture/data` for the static rows, `@uilc/fixture/generate` for the generator.
- `packages/criteria`: the 18 acceptance criteria from section 11, as one `runCriteria(name, createAdapter)` call, plus `expected.ts`, a reference implementation of the filtering and sorting the criteria compare against.
- `packages/harness`: the `ComparisonAdapter` interface, the `BuildResult` shape, and the 180 KB budget constant.
- `templates/common`, `templates/react`, `templates/vue`: the per-build template, common files plus a framework overlay.
- `scripts/roster.ts`: the eight builds with their library, framework, and kind. Both the scaffold and the scoring read it.
- `scripts/new-build.ts`: stamps a build folder. `pnpm new-build react-shadcn`.
- `scripts/measure.ts`: scores one build into `results/<build>.json`.
- `scripts/check-fixture.ts`: asserts the things the criteria assume about the fixture.
- `baselines/react`, `baselines/vue`: mount the framework, render nothing. Measured 44.91 KB and 24.21 KB gzipped, the `baselineGzipKb` every result file subtracts from. Never published.
- `.github/workflows/ci.yml`: a `shared` gate, then a matrix over the ten apps. A build leg skips itself until that folder exists.
- `builds/react-headless`: the first build, Headless UI on React. 18/18 criteria pass, driving the real DOM through the adapter rather than reading component state. Gzipped total 89.76 KB against a 44.91 KB baseline, a 44.85 KB delta, inside the 180 KB budget. `builds/react-headless/comparison.json` records that the modal came free from `Dialog`, the select and toast were hand built (Headless UI has no toast primitive), and 6 of section 9's requirements needed custom code. Scored into `results/react-headless.json`.
- `builds/react-mui`: Material UI on React. 18/18 criteria pass, no adapter method had to throw. Gzipped total 124.67 KB against the 44.91 KB baseline, a 79.76 KB delta, inside budget. `comparison.json` records `requirementsNeedingCustomCode: 3`, modal and select free from `Dialog`/`Select`, toast fully hand built (MUI's `Snackbar` is single-slot, not the 3-deep stack section 7 needs). Scored into `results/react-mui.json`.
- `builds/react-chakra`: Chakra UI (Ark UI under the hood) on React. 18/18 criteria pass, verified with a fresh test run. Gzipped total 142.71 KB against the 44.91 KB baseline, a 97.8 KB delta, inside budget. Modal and toast live-region/dismiss came free; toast eviction (cap at 3, oldest dropped first), row keyboard activation, `aria-sort`, table caption, and pagination live-region were hand built, 4 requirements total. Uses Chakra's `NativeSelect` (a real `&lt;select&gt;`) instead of the Ark listbox `Select`, a documented trade for multi-select reliability, not a limitation. Scored into `results/react-chakra.json`.
- `builds/vue-quasar`: Quasar on Vue. 18/18 criteria pass. Gzipped total 115.89 KB against a 24.21 KB Vue baseline, a 91.68 KB delta, inside budget. Modal and select came free from `QDialog`/`QSelect`; table and toast were hand built (`QTable` and `$q.notify()` didn't fit the spec's exact behavior), 6 requirements total, matching `react-headless`'s methodology for comparability. Caught and fixed a real critical axe violation (double-nested `&lt;label&gt;` around `QInput`) and a real app bug (a page-reset watcher ported from a React `useEffect` pattern that never fired on Vue's non-immediate `watch()`). Scored into `results/vue-quasar.json`.
- `builds/vue-vuetify`: Vuetify on Vue. 18/18 criteria pass, stable across three clean runs. Gzipped total 153.08 KB against a 24.21 KB Vue baseline, a 128.87 KB delta, inside budget. That figure was 120.17 KB total and a 95.96 KB delta until the section 7 pass found the build had never imported `vuetify/styles`; the higher number is the honest one. Caught and fixed two real application bugs (a `watch()` skip-first-run guard wrong for Vue's mount semantics, and the filter/table pipeline reading raw fixture data instead of the post-save `rows` ref) and moved dialog focus explicitly onto the first field, since Vuetify's own trap left focus on the triggering row. Scored into `results/vue-vuetify.json`.
- `builds/vue-primevue`: PrimeVue on Vue. 18/18 criteria pass. Gzipped total 174.39 KB against a 24.21 KB Vue baseline, a 150.18 KB delta, inside the 180 KB budget with about 5.6 KB headroom (needed trimming the theme to only the 10 mounted components and dropping an unused ~105 KB `primeicons` font import). Table, dialog, select, and confirm-dialog came free from `DataTable`/`Dialog`/`MultiSelect`/`ConfirmDialog`; toasts and modal focus placement/return were hand built (Dialog's focus trap and Escape listener never fired under this Jest/jsdom setup), `requirementsNeedingCustomCode: 4`. `axeViolationsBeforeFixes: 0`. Scored into `results/vue-primevue.json`.
- `builds/react-shadcn`: shadcn/ui (Radix) on React. 18/18 criteria pass, confirmed stable across three runs. Gzipped total 103.78 KB against the 44.91 KB baseline, a 58.87 KB delta, inside budget. `handBuilt: { modal: true, select: true, toast: true }`, `requirementsNeedingCustomCode: 4`, the whole table, the multi-selects (Radix's `Select` is single-value only, assembled instead from `DropdownMenu` + `Checkbox`), the toast cap-of-3, and explicit focus return to the originating row on modal close (Radix's `Dialog` only restores focus to a `Dialog.Trigger`, a no-op here since the dialog opens from controlled row state). `axeViolationsBeforeFixes: 0`. Scored into `results/react-shadcn.json`.
- `builds/react-antd`: Ant Design on React. 18/18 criteria pass, but **over the 180 KB bundle budget**: 278.65 KB gzipped total, a 233.74 KB delta against the 44.91 KB baseline, `overBudget: true` in the result. Isolated with a standalone esbuild bundle to confirm `Table` alone costs ~247 KB gzip with React (`rc-table` pulls in `rc-virtual-list` unconditionally), a genuine library-weight finding, not an implementation shortfall. Trimming elsewhere (native `<input type="date">` instead of `DatePicker`, plain markup instead of `Result`/`Skeleton`) saved real weight but couldn't close that gap. Modal and select came free; table `aria-label`, row keyboard activation, pagination bar, field error wiring, and toasts (entirely) were hand built, 5 requirements. Found a real library bug: `Select`'s open dropdown renders a hidden duplicate `role="option"` list alongside the real clickable rows, so a naive `getAllByRole('option')` matches the wrong copy. Scored into `results/react-antd.json`.
- `write-up/README.md`: the article, written 2026-09-04 by one owner reading `results/` and the eight `comparison.json` files. It follows section 10 of the spec: two reporting groups, delta as the only number quoted across them, shadcn/ui and Headless UI labeled assembly kits, the fixture generator with 10 sample rows, and no single winner. Time to first render is marked not measured. It closes on the four limits, so no number gets read for more than it is worth.
- `README.md`: rewritten 2026-09-04 around the results table. It had still described a repo where no build existed, and named a `templates/build` folder and a `test/adapter.ts` that do not exist. The freeze rules and the build workflow moved out to `CLAUDE.md` and this file.

## 3. The rules that govern edits

Phase one is the fixed input. Nobody working on a build edits `packages/criteria` or `packages/harness`. A criterion that looks wrong goes back to the phase one owner as a question, because a test patched locally ends the comparison and CI will not say so. The `builds` job diffs both packages against main for exactly this.

The slice is one library, never one feature. A feature slice would put eight workers inside the same eight folders. A worker writes only inside its own `builds/<name>/` and its own `results/<name>.json`.

`tickets.json` is committed and the seed is fixed. CI regenerates it and fails on a diff. If it ever changes, every earlier bundle and render number stops comparing and the run starts over.

Run `pnpm fixture:check` after touching the fixture. The criteria assume a row on each Created boundary, a search matching exactly one row, subjects spanning A to Z, and a status and priority pair with rows. That script asserts all of it.

A library that cannot meet a requirement records the failure. Do not bend the spec, and do not return a plausible fake from an adapter method to get the suite green. Throw instead. A thrown criterion is a recorded result.

## 4. Decisions made

- The fixture loads through a dynamic import inside `loadTickets`, so a bundler emits the 240 rows as their own chunk. Section 10 excludes the fixture from the size total, and `measure` can only do that if the rows are not inlined. A static import of `@uilc/fixture/data` in application code folds them back in and inflates the number. Tests import the static entry point freely.
- The generator is not re-exported from `@uilc/fixture`. It reads and writes files, and re-exporting it dragged `node:fs` into the browser bundle and broke `vite build`. It has its own entry point.
- `measure` reads the bundle and the ergonomics counts off the code. The two things a script cannot see, how many section 9 requirements needed custom code and whether the modal, select, and toast were hand built, come from a `comparison.json` each build declares.
- Lighthouse needs a served build and a browser, so a local `measure` writes `runs: 0` rather than inventing a number. CI supplies the median.
- A build with no `criteria-results.json` scores as 18 failures, not as 0 passes and 0 failures. A missing test run must never read as a clean one.
- The template splits into `common` plus a `react` or `vue` overlay. The two differ in the entry file, the Vite plugin, the dependencies, and a `.vue` type shim, and nothing else. A scaffolded build typechecks, builds, and measures before anyone writes a line.
- Each build's Vite config matches its baseline: no manual chunking, no external-ing the library. Either would move weight out of the number the comparison is about.
- The roster lives in `scripts/roster.ts` rather than inside the scaffold, because importing it from `new-build.ts` ran that script's CLI as a side effect.
- Each build pins its own dev server port in `vite.config.ts` under `server`, 5173 through 5180 in roster order (`react-headless` 5173, `react-shadcn` 5174, `react-mui` 5175, `react-chakra` 5176, `react-antd` 5177, `vue-vuetify` 5178, `vue-primevue` 5179, `vue-quasar` 5180), with `strictPort: true` so a collision fails loudly instead of silently landing on another build's screen. All eight can then run at once for side by side comparison. This only touches `server`, never `build`, so the bundle numbers are unaffected and the configs still match their baselines.
- The write-up publishes three of section 10's four categories and says plainly that time to first render was not measured, rather than waiting on a Lighthouse runner or quoting a local number. A missing measurement stated as missing is a result. An invented one would end the comparison the same way a patched test would.
- Process lives in one place each. `CLAUDE.md` holds the rules, this file holds the state and the reasoning, and `README.md` holds the results and links to both. The README used to restate the freeze rules and the build workflow, and the copy went stale first.

## 5. Gotchas

- Scaffolding a build needs `pnpm install` afterwards, or its workspace dependencies are not linked and everything reports as missing modules.
- `measure` identifies the fixture chunk by basename, matching `tickets-*.js` or `data-*.js`. A bundler that names chunks differently needs that regex updated, or the fixture counts against the budget.
- The criteria compare against `expected.ts` rather than against numbers typed by hand, so regenerating the fixture does not silently invalidate the suite. Keep it that way.
- `pnpm typecheck` covers the three shared packages only. A build typechecks through its own `tsconfig.json`.
- Files are written with LF newlines. Preserve that.
- The user's writing rules ban em dashes and a list of marketing words. Every markdown file here has none. Keep new prose in the same register.
- `packages/criteria/jest-preset.json` is invalid JSON: the pattern `"^.+\.(t|j)sx?$"` uses `\.`, which is not a legal JSON escape, so `JSON.parse` throws on it. Every build hits this. `react-headless` worked around it by inlining the same, correctly escaped settings directly in its own `jest.config.js` rather than touching the shared file. This is a phase one bug and belongs to the owner, not to per-build workarounds.
- The scaffolded test script assumes a hoisted `jest` binary (`../../node_modules/jest/bin/jest.js`), which does not match this pnpm workspace's layout. `react-headless` pointed its script at its own `node_modules/jest/bin/jest.js` instead.
- axe-core under jsdom cannot check color contrast (`HTMLCanvasElement.getContext` is not implemented in jsdom), which logs a harmless console error during the axe criteria. A "zero violations" result is real for what axe can evaluate under jsdom, but is not a substitute for a manual screen-reader pass or a browser-based axe run.
- Under Jest's `--experimental-vm-modules`, a default import from a CJS library subpath (e.g. `import Button from '@mui/material/Button'`) can resolve to the whole module object instead of the component, because Node's native CJS-to-ESM interop binds the default import to `module.exports` wholesale rather than `.default`. `react-mui` hit this and fixed it by switching to named imports from the library's barrel (`import { Button } from '@mui/material'`) in both app and test code. Watch for it in any build pulling components from CJS subpaths; it does not affect the Vite production build, only Jest.
- `react-chakra`'s Ark UI dialog/toast state machines schedule some updates on `requestAnimationFrame`, so its adapter waits for focus to actually land or return rather than for the element to appear or disappear. Its `createToaster()` is a module-level singleton that survives Jest's mount/unmount cycles, so its adapter calls `toaster.remove()` in `unmount()` to stop bleed between tests.
- `measure`'s "library imports" ergonomics count only sees explicit `import` statements. `vue-vuetify` (and, the same way, any Vue-suite build using `vite-plugin-vuetify`/`unplugin-vue-components`-style auto-import, likely `vue-primevue` and `vue-quasar` too) resolves `<v-btn>`, `<v-select>`, etc. from template tags at build time, so the count undercounts real library usage for those builds. This is a `measure`/phase one limitation, not a build bug; flagged for the owner rather than worked around.
- `vue-primevue`'s Jest setup needed more than the two react-headless workarounds: `@vue/vue3-jest` always downlevels a `.vue` file's compiled template to CommonJS via Babel regardless of Jest's ESM settings, and PrimeVue ships ESM-only with no `require` fallback, so the build runs Jest in CommonJS mode with a widened `transformIgnorePatterns` (documented in its `jest.config.js`). `@primevue/themes`'s `aura/<name>` subpaths are also import-only with no CJS fallback; worked around by depending on `@primeuix/themes` directly, which the `@primevue/themes` package itself re-exports from.
- PrimeVue's single-value `Select` commits an option on `mousedown`, while its `MultiSelect` sibling commits on `click`, so a one-size-fits-all "click the option" test helper silently no-ops on `Select`. Its `MultiSelect` combobox is also a visually hidden `<input>` whose own click handler ignores clicks on itself, so a test has to click its `[data-pc-section="root"]` ancestor, not the element `getByRole('combobox')` naturally returns.
- Killing a backgrounded `pnpm dev` from the agent harness on Windows stops the wrapper shell but leaves the child `vite` process holding the port, so the next start fails with "Port 5173 is already in use". Find the owner with `netstat -ano | grep ':5173'` and `taskkill //PID <pid> //F`.
- The Puppeteer MCP server drives ONE shared browser instance across every agent in a session. Running several agents that each navigate it at once makes them race: an agent screenshots and gets another build's screen back. Verify visually one agent at a time, or have each agent confirm `document.title` in the same call as the measurement it takes.
- A builder agent told to run the criteria suite will sometimes background its own test run, end its turn saying it will "report back when the notification arrives", and then wait forever, because nothing wakes it. Three of the eight builds did this. Tell builders to run the suite in the foreground, and if one returns a status message instead of results, message it back rather than assuming it failed.
- A build agent saying a fix "left the bundle unchanged" is not a measurement. Six of the eight section 7 fixes were reported that way and had in fact moved the number, by 0.01 to 0.26 KB, because nobody re-ran `measure`. Those are small, but they were wrong in `results/` for a day, and `vue-vuetify` in the same pass moved by 33 KB. Run `pnpm measure <build>` after any change that reaches the bundle and read the number rather than reasoning about it.
- The first CI run ever, on 2026-09-04, failed every build leg at `size, delta, and the budget`. `measure` reads `baselines/<framework>/dist` for `baselineGzipKb`, and the `builds` job built only its own app; the baselines build in a separate job on a separate runner, so `bundleOf` threw `no dist output, run build first`. Fixed by building the matching baseline in the build leg just before `measure`. Anything else `measure` reads off disk has the same shape of problem: the job has to produce it, not another job.
- `packages/criteria/jest-preset.json` was still invalid JSON on 2026-09-04. The rename did not touch it and no build stopped working around it.
- `axe-core` runs 2 to 4 real minutes per test against a heavier DOM (Ant Design's `rc-table` virtual-list scaffolding, `rc-select`'s duplicated option lists), and chained `Select` interactions in `react-antd`'s filter criteria pass Jest's 15 second default. Its `jest.config.js` raises `testTimeout` to 300000ms; this does not loosen any assertion, it only stops correct-but-slow interactions from racing the clock. Worth checking if it recurs on other suite-kind builds.
- The user works in very short exchanges, often ending a message with `yn` and asking for a one sentence answer. Answer the question asked, first, in one sentence, and put anything that needs a decision in a single follow-up line. Do not open with findings, and do not pad. The same goes for the prose rules in `~/.claude/CLAUDE.md`, which apply to chat replies as much as to files.
- Commits here go straight to `main`. There is no remote, there are no other branches, and every commit sits on `main`. Do not branch out of habit.

## 6. Not done

- ~~Nothing from 2026-09-03 is committed: the dev server port additions to all eight `vite.config.ts` files, the section 7 UI fixes across all eight builds, and the seven regenerated `results/*.json` files.~~ **Fixed.** All of it landed on 2026-09-04 in `65756f2` and `751f0de`, and the working tree is clean.
- `react-antd` passes all 18 criteria but is over the 180 KB bundle budget (233.74 KB delta), because Ant Design's `Table` alone costs roughly 247 KB gzip with React. That is a recorded result, not a defect to fix; the phase one owner decides what, if anything, that means for the comparison.
- No shared `criteria-results.json` reporter. Each build added its own local Jest reporter (see `builds/react-headless/test/reporter.cjs` for the pattern) rather than one shared implementation. The owner may want to promote it into a shared package now that eight builds have copied it.
- The `publish` job in CI is a placeholder. Nothing publishes the static sites yet.
- No Lighthouse runner. `measure` reads a `lighthouse.json` that nothing writes.
- ~~No write-up. `write-up/` is an empty folder.~~ **Written 2026-09-04** as `write-up/README.md`, from `results/` plus the per-build `comparison.json` notes. It covers bundle size, accessibility defaults and ergonomics, marks time to first render as not measured, and states the four limits: no contrast check under jsdom, no manual screen reader pass, no Lighthouse, and no review past first paint. Section 10 of the spec asks for four categories and only three have data, so it is complete only once a Lighthouse runner exists.
- Seven of the eight criteria suites have not been run since the rename. Only `react-headless` was re-run on 2026-09-04, passing 18 of 18 in 18 seconds. The rescore commit is the only evidence for the other seven. A full `pnpm -r --filter "./builds/*" test` has not run on the renamed tree. Budget hours for it: `react-antd` alone takes around 560 seconds.
- The remote is `https://github.com/BobDempsey/ui-library-comparison` (public), added 2026-09-04, and `main` pushed to it. `ci.yml` runs on push to `main` and on pull requests, so the first run fired on that push. No pull request has ever run, so the `builds` job's diff of `packages/criteria` and `packages/harness` against main is still untested; it only has meaning on a PR.
- Use Playwright MCP to fully test all builds' functionality (search, save, etc.). **The Playwright MCP server is connected as of 2026-09-04** and its browser tools are available, so nothing blocks this now. The survey in section 7 was done with the Puppeteer MCP server, which is also connected. Use Playwright: it is the better fit for this particular job, because its accessibility tree snapshots read `aria-sort`, live regions and focus position directly rather than inferring them from a screenshot, which is most of what section 9 asks about.
- Take screenshots of the eight apps when that Playwright pass is done, one per build off its own dev server port.
- Re-check the eight screens past first paint: open a modal, fire a toast, page through, and try a narrow viewport. Section 7 only covered the default state at 1440px, so whatever is wrong in those states is still unfound.
- Lint, format and typecheck every build. None of this runs today: `@biomejs/biome` is a root dev dependency but there is no `biome.json` anywhere and nothing invokes it, no build declares a `lint` or `format` script, and the root `typecheck` compiles only the three shared packages, so no build's own `tsconfig.json` has been checked since it was scaffolded. Whoever picks this up decides first whether the config is shared at the root or per build, and that is an owner call, since a root `biome.json` is shared phase one surface rather than something a build worker adds on its own.
- ~~Rename "bakeoff" to "ui library comparison" throughout, including the demo resume.~~ **Done 2026-09-04**, see section 8. One tail is left and it sits outside this repo: `../demo-resume/handoff.md` still says "bake-off" in 13 places and still describes a `bakeoff-screen-spec.md` that is no longer in that folder. Whoever owns the resume decides that, not a build worker.

## 7. UI defects found and fixed on 2026-09-03

A first pass over all eight screens at 1440px wide, first paint only. Nobody has opened a modal, fired a toast, paged, or checked a narrow viewport yet, so this was the visible surface of the default state and not a full audit. All eight are fixed, one agent per build, each confirming its own 18 criteria still pass. Committed on 2026-09-04 in `65756f2`.

Six of the eight agents reported their fix as leaving the bundle unchanged and had not actually rebuilt. A rescore on 2026-09-04 (`751f0de`) moved all six by 0.01 to 0.26 KB and corrected the line counts. The figures below are the rescored ones.

Two rules governed every fix. Differences in look between libraries are the finding the comparison exists to record, so a fix corrects something broken inside one build and never restyles a build to resemble another. And every fix re-runs `pnpm --filter @uilc/<name> test`, plus `measure` if it touched anything that lands in the bundle.

The pattern worth carrying forward: seven of the eight were a single missing or wrong line of setup, not accumulated sloppiness. Three builds were not loading something the library assumed (Vuetify's base stylesheet, an icon glyph, Chakra's width cap), and one was inheriting a default nobody chose (PrimeVue's system dark mode). Look for the one-line cause before writing CSS to cover a symptom.

- `react-headless` (5173): the `<legend>` of the Created between `<fieldset>` sat outside the flex flow and stretched the filter row, leaving a band of empty space above the other fields. Fixed by making the fieldset a plain block with a flexed inner wrapper and inline From and To labels. Bundle 89.73 to 89.76 KB total, 44.85 KB delta.
- `react-shadcn` (5174): the page container was `max-w-5xl`, about 150px too narrow for the filter row, so Created between and the Clear filters group wrapped to a second line under dead space. Widened to `max-w-7xl`. The Assignee caret was a literal `▾` character, and sortable headers showed no indicator until they were already sorted; both replaced with inline SVG chevrons in a new `src/components/ui/icons.tsx`, rather than adding `lucide-react` for three glyphs. Bundle 58.61 to 58.87 KB delta on the rescore.
- `react-mui` (5175): the Status, Priority and Assignee labels printed on top of their own placeholder text. `InputLabel` only shrinks on focus or a non-empty value, and with `displayEmpty` a placeholder still counts as empty, so the label never floated. Fixed with `shrink` on those three labels, matching what the date fields in the same form already did. Bundle 79.74 to 79.76 KB delta on the rescore.
- `react-chakra` (5176): the provider and style engine were mounted correctly, which the survey had guessed might be the cause and was not. Chakra's `Field.Root` and `Fieldset.Root` recipes set `width: 100%`, and the form capped fields only with `minW`, a floor rather than a ceiling, so every field's flex-basis resolved to the full row and wrapped one per line. That also made the native multi-selects render nearly 1400px wide, which is what read as unstyled. Fixed with explicit `flex` and `width="auto"` per field, plus a `Container` on the screen. Bundle 97.72 to 97.8 KB delta on the rescore.
- `react-antd` (5177): the native date inputs were styled to antd's own seed tokens (32px control height, 6px radius, `#d9d9d9` border, matching font) so they sit with the Selects, and the From and To labels moved inline so the group stops being a line taller than its neighbours. `DatePicker` was deliberately not adopted, since it costs about 37 KB gzip on a build already over budget. Bundle 233.45 to 233.74 KB delta on the rescore, still over.
- `vue-vuetify` (5178): `main.ts` never imported `vuetify/styles`. `vite-plugin-vuetify`'s auto-import resolves component styles but is not a substitute for the base stylesheet, so `VField`'s outlined-variant CSS never loaded and empty selects showed their label struck across the border with no caret. Fixed by importing it and switching to the `mdi-svg` iconset, which needs no font. Sortable headers were uppercase only because their label sits in a `v-btn`, whose recipe forces `text-transform: uppercase`; overridden with a scoped rule. Added a `v-container`. **This moved a recorded number: 120.17 to 153.08 KB total, 95.96 to 128.87 KB delta.** The old figure measured a build that was never loading the library's own CSS, so it understated Vuetify by about 33 KB. The new one is the honest number and it is still inside budget.
- `vue-primevue` (5179): PrimeVue's `options.darkModeSelector` defaults to `'system'`, so on a machine with a dark OS preference the preset's dark tokens matched and every themed control flipped, while the hand-written light colors in `styles.css` stayed put. That same switch put near-white text on the badges' fixed light-grey pills, which is where the unreadable contrast came from. Fixed with `darkModeSelector: false`, threaded through both `main.ts` and the test adapter so production and Jest configure PrimeVue identically. The trimmed preset and the dropped `primeicons` font were left alone. Bundle 150.17 to 150.18 KB delta on the rescore.
- `vue-quasar` (5180): no Material Icons font was ever loaded, so all six `QSelect`s printed the ligature name `arrow_drop_down` as text, and that 16-character string overflowing its icon slot was also what made the fields appear to overlap. Fixed by passing the caret as an SVG path string, which `QIcon`'s `isSvgPath` branch renders inline, so no font was added. Created between had the same fieldset legend bug as `react-headless` and got the same treatment, minus the extra `<label>` wrappers, which would have reintroduced an axe violation this build already fixed once. Bundle 91.63 to 91.68 KB delta on the rescore.

Two cross-build inconsistencies are deliberately **not** on the fix list. Badge labels are capitalized in `react-headless`, `react-shadcn` and `react-chakra` but lowercase in `react-mui`, `react-antd`, `vue-vuetify` and `vue-quasar`, and only `react-shadcn` renders a visible page heading, though the spec never asks for one. Both are library defaults showing through, which is exactly what the comparison is built to measure, so they stay as recorded findings for the write-up. Do not normalize them.

## 8. Renaming "bakeoff" to "ui library comparison"

**Done on 2026-09-04**, in one commit, `65756f2`. This section is kept as the record of what the names became, since the original instructions were written before the rename and the find and replace ran over them too, turning several "X becomes Y" lines into tautologies. What follows is the settled state, read back off the repo rather than off the old plan.

The names, as they now stand:

- Repo folder and root package name: `ui-library-comparison`.
- Package scope: `@uilc/*`, giving `@uilc/fixture`, `@uilc/criteria`, `@uilc/harness`, `@uilc/baseline-react`, `@uilc/baseline-vue`, and `@uilc/<build>` for all eight builds. The short scope was chosen over `@ui-library-comparison/*` to keep the many import lines readable.
- Per build declaration file: `comparison.json`, in all eight builds and in `templates/common/comparison.json.tmpl`. `scripts/measure.ts` reads that name.
- The adapter interface in `packages/harness` is `ComparisonAdapter`. `BuildResult` never carried the old name and did not change.
- The agent definitions are `.claude/agents/comparison-builder.md` and `.claude/agents/comparison-owner.md`, with matching frontmatter `name` fields, so they dispatch by those names.
- In prose, write "the comparison", never "the bake-off".

Verified on 2026-09-04 after the rename: `pnpm install` resolves every workspace dependency, `pnpm typecheck` passes, `pnpm fixture:check` passes all ten assertions, and `react-headless` passes 18 of 18. Case-insensitive grep for "bakeoff" and "bake-off" across the repo, excluding `node_modules` and `.git`, leaves hits only in this file, all of them deliberate references to the old name.

The one loose end is outside this repo. `../demo-resume/demo-resume.md` was updated and its heading now reads "UI library comparison, public write-up", but `../demo-resume/handoff.md` still says "bake-off" in 13 places and still lists a `bakeoff-screen-spec.md` that is no longer in that folder. That folder is not a git repository and is not part of this workspace, so it is the resume owner's call.

One caution if any part of this is ever redone. Do it in a single pass, never incrementally: a half-renamed workspace does not install, and a build whose dependency names no longer resolve reports every module as missing, which reads as a far worse problem than it is. And confirm afterwards that `results/*.json` still holds the same eight bundle numbers. A rename must not move them; if one moves, something else changed too and needs finding.
